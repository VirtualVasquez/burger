<li class="list-group-item list-group-item-info">
	{{burger_name}}

	{{#if devoured}}
	<button class="change-devoured btn btn-default" data-id="{{id}}" data-newdevoured="{{devoured}}">
		 Devour it! 
	</button>
	{{/if}}
</li>
